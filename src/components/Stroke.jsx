// Import React and Tailwind CSS
import '../styles/Stroke.css'; 

const Stroke = () => {
  return (
    <span className="flex items-center justify-center bg-white p-10">
      <h1 className="text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-black via-gray-700 to-slate-400">
        CodeSeek
      </h1>
    </span>
  );
};

// Export the Stroke component
export default Stroke;
